#!/usr/bin/env python
import os
from lawfactory_utils.urls import cache_file

file = cache_file() + '.sqlite'
if os.path.exists(file):
    os.remove(file)
    print(file, 'removed')
else:
    print(file, 'not found')
