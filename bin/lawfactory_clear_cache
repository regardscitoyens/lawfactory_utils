#!/usr/bin/env python
import os
from lawfactory_utils.urls import cache_file

if os.path.exists(cache_file()):
    os.remove(cache_file())
    print(cache_file(), 'removed')
else:
    print(cache_file(), 'not found')
